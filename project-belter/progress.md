Original prompt: Improve Project Belter: sharpen and simplify graphics while zooming out; show relative-velocity vectors only for the selected target; remove Hold Match; make sub-5 m/s collisions harmless and allow drones to launch; slow mining debris so it cannot disturb a ship stationary relative to the asteroid; add Q/E side-thrust controls and a mobile equivalent.

- New navigation/autopilot structure: navigation beacons and direction pointers are now standard ship equipment. Selecting a mapped destination without Wayfinder sets a persistent manual-flight beacon; it no longer engages autopilot for free.
- Internal Wayfinder conversion: the 220 CR flight-assist item is now the internal Wayfinder autopilot computer. It follows mapped routes and performs safe selected-asteroid approaches without occupying or appearing on a universal hardpoint. Old saves with Wayfinder mounted on a hardpoint migrate it into the internal bay and free that mount.
- Dense-field routing adjustment: base surface clearance is now 20 m. A regression corridor that is too narrow for the previous 42 m margin is accepted, while momentum clearance remains layered on at speed.
- Stable-route planning adjustment: route obstruction is still checked continuously, but a clear route that the ship is following within 16 m is retained for an 8-second stable interval instead of being regenerated every 2 seconds. New blockers still force immediate replanning.
- Corner tracking correction: smaller base clearance exposed aggressive corner cutting, so avoidance waypoints now require a 5 m capture and sharp turns slow much more strongly. Focused navigation tests retain full hull in the live field, the 35 m/s emergency intercept, and both large-blocker routes.
- Browser verification: the standard navigation map was inspected with Pallas Gate selected on a stock ship; it reports `NAV BEACON // MANUAL FLIGHT`, keeps the destination highlighted, and confirms autopilot is unavailable/inactive. The station screen was inspected with Wayfinder installed and shows `INTERNAL // INSTALLED` plus `NO HARDPOINT REQUIRED`, while the hangar remains at one of three physical mounts occupied.
- End-to-end verification: both desktop journeys pass. The mining/economy loop purchases Wayfinder with the first sale, persists it across reload, and confirms it appears in no hardpoint. The upgraded ship then crosses the live asteroid field to Pallas Gate at 50 m/s with full hull.
- Final verification: `npm run check` passes with 17 test files and 69 tests. The exact root standalone was rebuilt after the successful checks. Remaining TODO: none for this request.
- New zero-fuel propulsion correction: user reports that holding forward after fuel depletion does not produce usable motion from the two aft steering engines. Diagnosis: the independent force path exists, but the former 12 kN produces less than 1 m/s² on the empty starter ship and becomes nearly imperceptible with cargo.
- Zero-fuel propulsion implementation: raised the independent aft steering-engine forward force from 12 kN to 30 kN. Main-drive thrust and its yellow exhaust remain fuel-gated, while the two blue auxiliary aft jets now provide about 2.34 m/s² to the empty starter ship and remain useful with cargo.
- Regression coverage: added isolated tests for accelerating from rest with an empty tank and for continuing to accelerate while forward remains held across the exact moment fuel reaches zero. Both verify retained forward input, increasing speed, and fuel staying at zero.
- Zero-fuel visual verification: a deterministic browser scenario launches the exact standalone, empties the tank, holds auxiliary forward thrust, and reaches about 7 m/s after three simulated seconds. The inspected screenshot shows the blue aft steering jets active without the yellow main-drive plume. The standard browser-game client also launched the rebuilt standalone with clean text state and no reported page errors.
- Final zero-fuel verification: `npm run check` passes with 17 test files and 65 tests, and the exact root standalone was rebuilt after the successful check.
- New autopilot correction: the user supplied a screenshot showing a later route segment crossing directly through a very-large asteroid. Requested more waypoints, smaller clearance so dense passages remain routable, and 50 m/s travel speed.
- Route-integrity correction: autopilot now validates every remaining displayed leg on every update, rather than checking only the immediate waypoint. A moving asteroid obstructing any later leg triggers an immediate replan. Grid expansion also rejects edges that cut through an obstacle envelope, and fallback routes are accepted only when the complete multi-leg path is collision-clear.
- Waypoint/clearance tuning: route smoothing now keeps a waypoint at least every 320 m, producing visible multi-point bends around large bodies. Grid resolution improved from 64 m to 48 m. Base surface clearance dropped from 80 m to 42 m so narrow passages remain searchable, while speed-dependent momentum clearance remains available for emergency braking.
- Speed tuning: autopilot cruise is now 50 m/s, with 14 m/s retained through the sharpest corners. The very-large-asteroid stress route reaches above 47 m/s and completes without damage.
- Final verification: `npm run check` passes with 17 test files and 63 tests. New coverage reproduces the screenshot-scale Atlas geometry, verifies every displayed segment, verifies later-leg obstruction replanning, retains the 35 m/s emergency recovery test, and completes the live-sector Pallas crossing with full hull. The exact standalone was rebuilt. A wide-view system-Chrome screenshot was inspected and shows the orange route bending through multiple waypoints around the colossal icy asteroid without crossing its sprite or contour. The required generic browser client also launched without console/page errors; its WebGL-only screenshot remains black.
- Remaining TODO: none for this correction.
- New request: add saved pause-menu sliders for stationary and maximum-speed camera zoom, allow substantially wider phone views, normalize velocity-arrow length to 100 m/s, increase arrow weight and change it from yellow to red above that point with a pre-limit warning, and cap ship speed at 150 m/s.
- New request: substantially improve autopilot speed and reliability around asteroids. Initial diagnosis: route validity is purely geometric, so it ignores ship momentum and braking distance; smoothed waypoints also use destination-style slowdown, producing late avoidance turns followed by unnecessary crawling.
- Autopilot control pass: route envelopes now expand with current ship speed, fixed asteroid clearance increased from 34 m to 80 m, and moving-obstacle prediction extends from 3.5 to 5 seconds. Transit waypoints retain useful speed instead of applying destination braking; corner speed follows a braking curve based on turn sharpness and remaining distance.
- Flight-control pass: assisted flight now uses lateral thrusters to cancel cross-track velocity while the hull rotates toward the new course. Cruise speed increased from 30 to 42 m/s, with at least 12 m/s retained through the sharpest planned corners. Valid routes are reused for two seconds while any newly obstructed segment still triggers an immediate replan, preventing dense-field planning from becoming a frame-time problem.
- Regression coverage: a 1.3 km route around a stationary 300 m-radius asteroid completes in under 60 simulated seconds, exceeds 34 m/s, keeps over 45 m of surface clearance, and preserves full hull. A separate 35 m/s head-on engagement test confirms the autopilot can recover from dangerous existing momentum and still arrive undamaged.
- Final verification: `npm run check` passes with 57 tests, including the stress route, emergency collision-course recovery, and a complete live-sector crossing/dock at Pallas Gate with 100% hull. The exact root standalone was rebuilt. The required generic browser client launched the standalone and reported clean flight state with no console/page errors; its WebGL screenshot remains black. The normal Playwright screenshot runner again failed before page load with the environment's intermittent Chromium `SIGTRAP`, so the new desktop autopilot browser journey is present and syntax-checked but could not produce its intended rendered route screenshot in this run.
- Remaining TODO: hands-on feel tuning can adjust the 42 m/s cruise ceiling, but the current value is the highest tested setting that preserved full hull through the dense live-sector route.

- Initial audit: the game uses Phaser views over a fixed-step simulation. The canvas currently renders at CSS-pixel resolution, matching the visible softness on high-density screens. Input has rotation/forward/reverse but no lateral-thrust action. `HOLD MATCH` is a separate HUD/Shift action. Collision safety is currently 2 m/s, while mining arming requires under 2 m/s. Mining debris ejects at 7–14 m/s relative to its source asteroid.
- Implementation plan: enable capped high-DPI rendering and reduce camera zoom; simplify asteroid/ship/world overlays; remove match-hold UI/input path; add lateral thrust to the shared action layer with Q/E keyboard and paired mobile buttons; raise harmless-contact/mining thresholds to 5 m/s; make mining debris slow and non-disruptive near its source operation; add focused tests and browser QA.
- Mechanics implemented: Q/E now apply true lateral force without rotation; paired mobile hold buttons use the same shared action; Hold Match and Shift matching were removed. Contact below 5 m/s is fully inert, mining can arm below 5 m/s, and mining fragments eject at only 0.65–2.2 m/s plus a small tangent component relative to their source asteroid.
- Visual pass implemented: camera zoom is lower, detailed raster sprites were replaced in flight by clean Phaser-drawn silhouettes, and the starfield was reduced. The relative-velocity vector is drawn only by `NavigationView` for the current selected target; the ship retains its separate own-velocity arrow.
- Verification so far: typecheck, lint, 24 unit tests, production build, and the original desktop/mobile Playwright journeys pass. Fresh screenshots were inspected; the simplified objects remain clear at the wider zoom and mobile controls fit between the joystick and context actions. The required generic web-game client drove the game and captured correct text state, but its direct WebGL `toDataURL` screenshots were black in both headless and headed mode; the project's CDP full-page Playwright screenshots rendered correctly.
- Delivery clarification: the user identified `project-belter-vertical-slice-standalone.html` in the project root as the functioning file. The standalone builder now writes both the dist artifact and that root file so the requested changes land in the actual delivery target.
- Final verification: `npm run check` passes (typecheck, lint, 24 unit tests, production build). The expanded Playwright suite passes its mobile journey, full desktop mining/docking/economy journey, Q/E keyboard strafe check, mobile button strafe check, and a direct `file://` launch of the exact root standalone artifact. The standalone test also confirms `render_game_to_text`, no `HOLD MATCH` UI, no page errors, and unchanged heading during lateral thrust. The final standalone screenshot was visually inspected.
- TODOs: none for this request. The only build note is Vite's existing Phaser bundle-size warning.
- Sprite direction approved: a clean middle ground between classic frontier space-trader art and restrained hard-science industrial design. Large objects keep more structure; small objects use simpler silhouettes.
- Production sprite pass: added a detailed ship and station, a miner drone with a clear forward twin-prong mining tool, three material-specific asteroid sprites, and a simple debris fragment. Chroma-key backgrounds were removed, alpha edges checked, and assets were downscaled for runtime use.
- Integration pass: flight views now use the new raster sprites while retaining procedural thrusters, selection rings, damage tinting, docking progress, and mining-state glows. The HUD and station interface now use warmer gunmetal panels, tactile controls, restrained cyan/amber accents, and simpler industrial borders to match the sprites.
- Sprite integration verification: `npm run check` passes again (typecheck, lint, 24 unit tests, production build). Browser screenshot and interaction QA still pending.
- Browser QA: the full Playwright suite passes for desktop, mobile, mining, docking/economy, Q/E and mobile lateral thrust, and the exact root standalone file. Fresh desktop and mobile screenshots were inspected; the station detail, object hierarchy, target rings, and warmer industrial UI remain clear at the wider camera scale. Drone display scale was raised slightly after inspection so its forward mining prongs remain legible during deployment without changing its physics.
- Final verification: `npm run verify` passes after the drone scale adjustment (24 unit tests plus all applicable desktop/mobile Playwright journeys). The root `project-belter-vertical-slice-standalone.html` was rebuilt with the final sprites and UI. Remaining TODOs: none; further art tuning can proceed from this integrated baseline.
- New request: use the compact single-drill drone from the user reference; improve the station interface with a visual 1990s space-trader feel; make Assist a purchasable station upgrade; use sprite icons for upgrades; and integrate visible ship hardware cleanly instead of drawing modules above the hull.
- Asset pass: generated and alpha-processed a reference-matched mining drone plus paired engine, retro, cargo, and avionics module sprites. The hardware assets are designed to work both as outfitter thumbnails and layered ship hardpoints.
- Feature implementation in progress: Assist is now gated by a new Wayfinder station module, the station includes a dock viewport and live loadout preview, outfitter cards use actual sprite icons, and the flight ship uses fitted raster hardware with dedicated mounting scale/position and drive-pod exhaust.
- Final station/upgrade pass: the desktop station is now a framed trade terminal with a station viewport, commodity exchange, hangar ship preview, and outfitters. Mobile keeps the same hierarchy in a scrollable service layout. Module thumbnails were enlarged after screenshot review.
- Final ship fit: cargo saddles sit widest behind the hull, large drive pods occupy the aft side hardpoints with their own exhaust, retro units sit over the upper shoulder mounts, and the avionics unit sits on the centerline. A four-module browser scenario verifies that the combined silhouette remains coherent in the hangar and in flight.
- Final mechanics and verification: Assist stays locked and reports `ASSIST UPGRADE REQUIRED` until the 220 CR Wayfinder module is bought and installed; manual station return remains available. `npm run verify` passes with 26 unit tests and all applicable desktop/mobile/standalone Playwright journeys. The exact root standalone file was rebuilt. Remaining TODOs: none for this request.
- New request: deepen mining with visibly different asteroid shapes, resource availability tiers independent of size, asteroid collisions, and rare breakups into smaller asteroids of the same family.
- Mining-depth implementation: generated and integrated twelve clean asteroid sprites covering four silhouettes for each carbonaceous, icy, and metallic family. Added trace, standard, rich, and exceptional deposits; the target panel now reports family, shape, tier, remaining yield, and stability. Depletion fades the resource glint and reveals mining scars.
- Physics implementation: asteroids now collide and exchange momentum. Sufficiently energetic contacts can deterministically trigger a stability- and material-weighted fracture; fragments inherit the parent family, resource, tier, motion, spin, and 94% of its remaining deposit. Fragment generations, a short collision grace period, minimum parent size, and a global count cap prevent immediate chain reactions and runaway entity growth. High-energy ship impacts use the same fracture rules.
- Automated coverage added for size-independent deposit tiers, shape assignment, low-speed non-fracture, same-type fragment inheritance, resource conservation, and selected-target transfer to the largest fragment. Current result: typecheck, lint, and 29 unit tests pass. Browser and standalone verification still pending.
- Final verification: `npm run check` passes with 30 unit/integration tests and a production build. The required web-game client launched the rebuilt exact standalone, produced clean text state with material/shape/tier/yield metadata, and observed a natural same-family generation-1 breakup without runtime errors. Its WebGL-only screenshots remained black, as in the prior project pass.
- Browser-runner note: the full Playwright project suite could not start Chromium in this environment; every case failed before page load with the browser process receiving `SIGTRAP`, including a serial retry. The in-app browser also blocks automated access to local `file://` pages. This is an environment-level visual-QA gap, not an assertion or page-runtime failure. The generated sprite atlas and extracted transparent variants were visually inspected directly.
- Delivery complete: the root `project-belter-vertical-slice-standalone.html` contains the new v3 asteroid assets, deposit tiers, collision/fracture system, target telemetry, and depletion presentation. Remaining TODO: rerun the normal desktop/mobile Playwright journeys when Chromium launch is available.
- New request: introduce true small, medium, and large asteroids at 1×, 2×, and 5× scale, reuse the current sprites, visualize their collision contours, and establish large bodies as future cover geometry.
- Size/cover pass complete: asteroid entities now carry an explicit size class. The reference bodies are exactly 12 m, 24 m, and 60 m radius, while seeded field bodies use the same 1×/2×/5× bands with ±12% variation and a 56%/32%/12% distribution. Random placement now avoids spawning inside another asteroid's collision radius.
- Presentation complete: every asteroid draws a dashed physical collision contour with size-coded weight and color; target telemetry and `render_game_to_text` report the size class. M-12 is now the guaranteed medium reference and R-03 Black Dividend is a nearby 60 m large landmark with a 96 t exceptional deposit.
- Fragment integration complete: child radius automatically determines its new size class, so large bodies break down into genuinely smaller cover pieces while preserving the existing family/resource rules.
- Verification: typecheck, lint, 31 unit/integration tests, production build, exact standalone rebuild, and required web-game-client state checks pass. The state output confirms nearby 12 m, 24 m, and 60 m bodies and a 38-body non-overlapping field. Both headless and headed WebGL canvas captures remain black in the generic client; the separate Playwright suite still cannot start its Chromium shell and exits with the existing pre-page-load `SIGTRAP` environment failure.
- New size and contour pass: reclassified the existing 12 m, 24 m, and 60 m bodies as very-small, small, and medium. Added guaranteed 120 m large and 300 m very-large landmarks, kept seeded variation across all five bands, and doubled the in-world station sprite and collision radius from 55 m to 110 m.
- Collision contours now use four convex profiles fitted to the boulder, shard, rubble, and wedge sprite silhouettes. The same rotated outline drives rendering, ship impacts, asteroid impacts, target picking, mining surface distance, and drone attachment.
- Verification: `npm run check` passes with 34 tests and the exact standalone was rebuilt. The required web-game client launched it without runtime errors; state confirms 24 m small, 60 m medium, and 120 m large bodies in the near field while unit coverage confirms the fixed 300 m very-large body. The generic client's WebGL screenshot remains black as in prior passes.
- Contour correction: replaced the shared hand-authored polygons with 48-point profiles generated directly from every runtime sprite alpha mask, separated by material and by standard/colossal resolution. The contour now renders above the asteroid art as an always-visible alignment overlay, and the same radial profile remains the physical surface used by impacts, targeting, mining distance, and drone attachment.
- Colossal art pass: generated three 2×2 high-resolution sheets with the built-in image workflow, removed chroma backgrounds, split and cleaned them into twelve 627 px sprites, and reserved them for very-large asteroids. Static contour-over-sprite previews for all 24 sprite/profile combinations were rendered and visually inspected.
- Preserved behaviors: contact below 5 m/s remains inert and permits ship/asteroid overlap; the station remains absent from ship collision resolution. Added a regression test for the station pass-through behavior.
- Final verification: `npm run check` passes with 35 tests, the exact root standalone was rebuilt with all twelve colossal sprites, and the required game client launched it without console/page errors. Its WebGL capture remains black; the in-app browser also blocks automated access to local `file://` pages. Six direct sprite-plus-contour preview sheets provide the visual alignment check and show the alpha-derived line following the rendered edges.
- New request: make ship/asteroid impacts substantially more forgiving, let drones deploy sooner while the ship remains near a target, and replace the direct Return action with a manual navigation map.
- Flight tuning pass: asteroid damage now uses closing speed, softer response, gentler damage bands, and a 30-point per-impact cap. Contact below 5 m/s remains fully inert. Mining now arms within 70 m below 6.5 m/s after 0.6 seconds, with a wider pause and recall envelope.
- Navigation pass: `NAV` opens a sector map with three stations and three named asteroid fields. Selecting a station sets a persistent direction-and-range beacon while leaving the current asteroid target intact and disengaging approach assistance. It never changes ship velocity or performs route planning.
- Final verification: `npm run check` passes with 40 tests and the exact root standalone was rebuilt. The required game client opened the navigation map, reported all six destinations, selected Pallas Gate, and confirmed a persistent 2.24 km beacon with unchanged manual flight and no runtime errors. Its WebGL-only screenshot remains black as in prior passes.
- New request: add a second physical station and turn map selection into a complete collision-avoiding autopilot for both stations and asteroid-field nav points.
- Navigation architecture implemented: Ceres Relay and Pallas Gate now exist as separate in-world station entities, can be targeted and docked independently, and update the station interface identity after arrival. The map contains those two stations plus three selectable field points.
- Autopilot implementation: map selection immediately engages a dynamic grid A* planner with line-of-sight path smoothing, expanded ship/asteroid clearance, 3.5-second obstacle-motion prediction, 0.45-second replanning, safe-goal correction, and a fallback detour. Route waypoints feed the existing physical steering and thrust controller; direct player input disengages the autopilot.
- Coverage so far: targeted navigation tests verify physical station creation, safe planned segments around a moving large obstacle, manual override, field arrival, a full physical detour around a route-blocking asteroid, and a 2.2 km live-sector crossing with automatic docking at Pallas Gate and no hull damage.
- Final verification: `npm run check` passes with 43 tests, including the complete Pallas crossing and docking journey. The exact root standalone was rebuilt. The required browser game client selected both Kestrel Field and Pallas Gate through the visible map controls, confirmed active avoidance routes, normal fuel-using flight, both physical station entities, full debug route telemetry, 100% hull, and no runtime errors. Its WebGL-only screenshots remain black as in earlier passes, so route visuals could not be inspected through that client.
- Remaining TODO: none for this request. Future tuning can reduce or increase the 34 m safety margin and 30 m/s cruise limit after hands-on feel testing.
- New request: replace the additive upgrade silhouette with the user-supplied Mule/Hauler sprite, three fixed universal hardpoints, a central drone cargo hatch, visible vulnerable equipment, and a collision contour fitted to the new hull.
- Hauler asset pass: preserved the supplied ship art and removed its baked checkerboard locally, producing a transparent 684×1230 runtime sprite. A repeatable preparation script derives a 96-point radial hull profile plus port, starboard, ventral, and cargo-bay anchors directly from that asset.
- Hardpoint implementation: the starting loadout is one ventral mining drone with both side mounts empty. All three mounts accept any current equipment type, dock controls can move or clear equipment, and mounted modules render at their fixed physical anchors. Clearing/replacing the drone supports zero-drone builds.
- Drone loop implementation: launched drones vacate their visible hardpoint, mine, return through the central cargo hatch, unload, and either fly back to the asteroid or berth at their assigned mount. The cargo hatch highlights while unloading.
- Damage and collision implementation: the alpha-derived ship profile now drives asteroid contact distance and mining surface distance instead of the old circular ship radius. The same contour is drawn over the ship for visual verification. Damaging impacts also reduce the condition of exposed equipment on the struck side; disabled equipment stops contributing stats or drone capacity until serviced.
- Station interface pass: the outfitter now shows the new hauler at its real proportions, fixed hardpoint equipment, condition state, per-module mounting controls, and explicit clear-hardpoint actions.
- Inventory completion: equipment ownership now tracks individual copies. Players can buy and mount a second or third mining drone, stack duplicate drive/cargo/relay hardware if desired, or keep purchased items in station storage. The outfitter reports owned and mounted counts for each equipment type.
- Final verification: typecheck and lint pass with 50 unit/integration tests, including the one-drone starting layout, saved zero-drone builds, three-drone deployment from all universal mounts, duplicate-equipment purchases, alpha-profile side contact, and side-specific equipment damage. The exact root standalone was rebuilt.
- Browser QA: the generic web-game runner still exits before page load with the environment's existing Chromium `SIGTRAP`. A system-Chrome visual journey successfully opened the exact standalone, launched, selected and approached M-12, observed a full mining return, and captured the cargo hatch during unloading with no console or page errors. Desktop and mobile station renders plus flight/unloading renders were inspected; the hauler remains sharp, the contour hugs the hull, fixed mounts read clearly, and the mobile station layout remains usable.
- Remaining TODO: none for this request.
- Camera customization complete: the pause menu now exposes saved `0 M/S ZOOM` and `150 M/S ZOOM` sliders. They scale the viewport-specific base zoom from 30–130% and 22–130%, respectively, so phone players can pull substantially farther back. Dynamic zoom interpolates across the full 0–150 m/s range; disabling it uses the stationary setting.
- Velocity presentation complete: the ship velocity arrow reaches full length at 100 m/s. From 100–150 m/s it grows from 0.9 to 3.7 px in weight, changes cyan → yellow → red, shows `HIGH VELOCITY` from 130 m/s, and changes to `MAX SPEED LIMIT // 150 M/S` from 145 m/s. The HUD speed readout mirrors the warning colors.
- Physics limit complete: all ship velocity is now hard-clamped to 150 m/s.
- Final verification: `npm run check` passes with 17 test files and 61 tests, including zoom interpolation, settings persistence at the widest values, velocity-arrow thresholds, and the 150 m/s clamp. The exact root standalone was rebuilt. Desktop and mobile-breakpoint pause-menu screenshots plus yellow 135 m/s and red 147 m/s flight screenshots were inspected; the controls fit, their values remain readable, the wide zoom visibly pulls the scene back, and both warning stages are clear. The required generic client also launched the rebuilt standalone with clean text state and no console/page errors; its WebGL-only capture remains black.
- Remaining TODO: none for this request.
- Final joystick/plume verification: the virtual knob now preserves its CSS centering offset during movement and reset; system-Chrome measurement reports an exact `0 px / 0 px` centre delta after release. Low-power forward, reverse, and turn captures confirm that plume geometry grows from fixed nozzle-local origins instead of shifting away from the hull.
- Automated verification: `npm run check` passes with all 55 tests, lint, typecheck, and the production build. The exact standalone was rebuilt. The generic game client still intermittently exits with the existing pre-page-load Chromium `SIGTRAP`; the system-Chrome visual run completed without console or page errors.
- Remaining TODO: none for the joystick and constant plume-attachment request.
- New sector request: move Pallas Gate to the upper-left and Miner's Rest to the lower sector with at least 1 km asteroid clearance, replace the scattered field with a diagonal belt that is sparse through the centre and denser/larger/more energetic/richer in its lower-left and upper-right ends, make barren rock the dominant asteroid population, and add an NPC trader shuttle.
- Sector implementation: the deterministic 42-body field now follows the diagonal belt zones. Barren `rocky` bodies reuse all four established silhouettes, carry no recoverable yield, cannot launch mining drones, and report clearly as barren in the target/objective UI. Resource-bearing corner asteroids are biased toward higher tiers and larger sizes; corner velocity/spin is higher than the open centre. Both stations keep a 1 km surface-clearance exclusion.
- Trader implementation: Free Trader Lark uses the player hauler sprite, waits five seconds at each station, then follows a collision-checked multi-leg route to the other at 36 m/s. It replans around moving asteroids, appears in flight and on the sector map, and exposes its state and route in debug text. Unit coverage verifies a full outbound docking and return departure. Visual QA shows the open centre, large lower-left belt bodies, relocated stations, the diagonal map band, and the live trader marker.
- Final verification: `npm run check` passes with 18 test files and 72 tests. The desktop sector-map browser regression and the exact root standalone launch test both pass in system Chrome. The final standalone was rebuilt after the trader-label cleanup. Remaining TODO: none for this request.
- New request: suppress shake/announcements for non-player impacts, calm asteroid drift, give barren rock its own art, make independent ships selectable and navigable with fitted contours, make map selection plot rather than automatically engage autopilot, and give very-large asteroids durable structure plus map tracking.
- Barren-rock art pass: generated a four-shape natural basalt family with the built-in image workflow, removed the green key locally, produced 256 px standard and 627 px colossal runtime sprites, and regenerated alpha-fitted contours for all eight assets. Direct contour previews show the physical outlines closely following the new boulder, shard, rubble, and wedge silhouettes.
- Navigation/contact pass: map selection now plots and continuously updates a collision-safe guide route but never starts Wayfinder automatically. An explicit map button engages/disengages autopilot. Free Trader Lark and every current very-large asteroid appear as clickable map contacts; traders can also be clicked directly in flight. Dynamic trader routes update with its transponder position.
- Collision/presentation pass: the trader uses the alpha-derived hauler hull contour for rendering, picking, physical player collisions, and autopilot avoidance. Non-player asteroid fractures still happen physically but no longer produce camera shake or HUD announcements; those effects are reserved for player collisions. Mobile hides compact-map contact labels while retaining the full clickable directory.
- Asteroid tuning: average centre drift is below 1.5 m/s and violent-corner drift below 3.5 m/s. Asteroids now have size- and material-scaled structural integrity; the rocky very-large Atlas massif starts above 80 structural points and survives a severe first impact. Target/map telemetry reports remaining structure.
- Final verification: `npm run check` passes with 18 test files and 76 tests. System-Chrome tests pass for the exact standalone, desktop route plotting/explicit autopilot, clickable trader/massif contacts, selected-trader contour, and mobile contact layout. The required generic browser-game client was attempted twice but its bundled Chromium again exited before page load with the known macOS `SIGTRAP`; system Chrome completed all equivalent checks without page or console errors. The exact root standalone was rebuilt. Remaining TODO: none for this request.
- New route-visual correction: manual guide waypoints now retire when the ship crosses the perpendicular plane through a waypoint, rather than remaining until the ship comes within the 5 m capture radius. This removes passed points promptly even when the player flies well off the guide line; the remaining route is still checked and replanned if skipping the point exposes an unsafe leg.
- Main-plume overlay correction: the user screenshot showed the sprite's black exhaust-divider line still visible through the plume base. Added a fully opaque orange combustion cap spanning the complete nozzle opening, plus a bright inner oval, before blending into the yellow plume. This remains above the hull sprite. Verification pending.
- Enlarged QA found the cyan collision contour was still composited over the new cap. The main-drive plume has now been moved to the final/topmost ship layer so it covers the sprite divider and the contour at the exhaust opening.
- Final verification: the enlarged rebuilt capture shows an uninterrupted yellow-white nozzle opening with neither the black sprite divider nor cyan contour crossing it. `npm run check` passes with all 54 tests, and the exact standalone remains rebuilt. Remaining TODO: none.
- New request: recenter the virtual joystick and keep every plume physically attached to its nozzle at partial analog thrust.
- Attachment refactor: each main, reverse, auxiliary, and side plume now has its own object-space origin at the nozzle. Strength scaling occurs outward from that local origin, so the ship-side edge remains fixed from minimum visible input through full thrust. The joystick knob now always applies its required `-50%/-50%` centering transform before pointer displacement, including after release/reset. Verification pending.
- Thruster correction request: place the main-drive glow above the sprite, realign the forward RCS ports, restore visible aft RCS ports, and reduce the steering plumes to roughly half their prior dimensions.
- Correction implemented: the orange engine halo and core now render after the hull sprite. All four RCS effects also render above the hull, use sprite-derived fore/aft positions at y -13.7 and +11.7, start at the side-shell edge, and are approximately 50% of the former plume length and width.
- Final verification: typecheck, lint, all 53 tests, production build, and exact standalone rebuild pass. The generic browser client briefly completed once but reverted to the existing intermittent Chromium `SIGTRAP` after rebuild. System-Chrome captures of main thrust, both turn directions, and both strafe directions contain no console/page errors. Visual inspection confirms the main glow is visible over the nozzle, both aft jets render, the fore jets align with their yellow side housings, and all four steering plumes are half-sized.
- Remaining TODO: none for this correction.
- New propulsion correction: align the reverse-thrust plumes to the front shoulders, restore a large yellow main plume above the sprite, and add two small blue outer-aft auxiliary drives that provide limited forward motion even with an empty fuel tank.
- Implementation complete: the reverse pair now begins at x ±13.95 / y -23.7 instead of floating above the nose. The central yellow plume overlays the built-in main nozzle. Two blue auxiliary plumes sit at x ±14.55 / y +25.2 and fire with every forward command. Flight physics now preserves steering, strafing, and 12 kN auxiliary forward thrust at zero fuel, while the main yellow drive and reverse drive still require fuel.
- Final verification: `npm run check` passes with 54 tests and the exact standalone was rebuilt. The required generic client again hit the intermittent Chromium `SIGTRAP`; system-Chrome flight captures completed with no console/page errors. Enlarged inspection confirms the reverse jets attach to the front shoulder nozzles, the large yellow plume begins within the central main exhaust and renders above it, and both small blue auxiliary plumes attach to the outer aft rail ends.
- Zero-fuel behavior verified by regression test: steering, strafing, and 12 kN auxiliary forward thrust remain available at zero fuel; the yellow main drive and reverse drive correctly remain unavailable.
- Remaining TODO: none for this request.
- New request: refit the hauler propulsion effects so the main drive glows without a plume, add two small blue steering plumes on each side, and use diagonal fore/aft jet pairs to show rotation around the ship centre.
- Thruster rendering pass: removed the long main-engine flame and replaced it with a stronger orange halo plus a bright core over the built-in drive nozzle. Added forward and aft RCS plumes at the visible side-thruster positions on both sides of the hull. Clockwise steering fires forward-port plus aft-starboard; counter-clockwise steering mirrors the pair. Lateral translation fires both jets on the side opposite the requested force. Reverse thrust retains two compact blue nose jets.
- Final verification: `npm run check` passes with 53 tests and the exact standalone was rebuilt. The required generic browser client still fails before page load with the existing Chromium `SIGTRAP`. System-Chrome QA captured main thrust, both turn directions, and both strafe directions with no console/page errors. Visual inspection confirms the main engine now brightens without a plume, the four RCS jets sit on the side-port silhouette, turns show the correct opposing fore/aft pair, and strafing shows both jets on one side.
- Remaining TODO: none for this request.
- Navigation guide correction: manual-route waypoints now retire as soon as the ship crosses the perpendicular plane through them, even if the player passes well away from the guide line. The remaining route is still checked and replanned when skipping the old point exposes an unsafe leg.
- Verification: `npm run check` passes with 77 tests, the exact standalone was rebuilt, the bundled browser smoke client completed without runtime errors, and the focused desktop Chrome regression passed. The capture confirms the passed waypoint no longer remains behind the ship.
